kind = "data-app"
previewPath = "/"
title = "Python Data App"
version = "1.0.0"
id = "artifacts/data-app"
router = "path"

[[integratedSkills]]
name = "data-visualization"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 5000

[services.development]
run = "cd /home/runner/workspace/artifacts/data-app && streamlit run app.py --server.port 5000"

[services.env]
PORT = "5000"
